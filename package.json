{
  "name": "capture-for-agents",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "check": "tsc --noEmit",
    "lint": "eslint --quiet src"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "eslint": "^9",
    "typescript": "~5.6.2",
    "typescript-eslint": "^8",
    "vite": "^6.0.3"
  }
}
